{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "check-types": {},
    "test": {},
    "lint": {},
    "build": {
      "dependsOn": ["check-types", "^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
