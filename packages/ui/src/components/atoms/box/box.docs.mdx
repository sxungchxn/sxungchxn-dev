import * as BoxStories from './box.stories'
import {Canvas, Controls, Description, Meta, Story, Title} from '@storybook/blocks'

<Meta of={BoxStories} />

<Title/>

<Description/>

<Canvas of={BoxStories.Default}/>

<Canvas of={BoxStories.SlotExample}/>

## 주의사항

- `Box` 는 주로 내부적으로 공통 컴포넌트를 만들어낼때 사용됩니다.
- 단순 레이아웃이 목적이라면 이것을 사용하지 말고 대신 `Flex` 이나 `Grid` 컴포넌트를 최대한 사용해보세요.
- `asChild`를 이용하여 자식 컴포넌트에게 prop을 전파할 수 있습니다.

<Controls/>
