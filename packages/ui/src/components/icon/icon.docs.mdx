import * as IconStories from './icon.stories'
import {IconGallery, Meta, Source} from "@storybook/blocks";
import {Icon} from "./icon";
import {icons} from "@seungchan-dev/icons";

<Meta of={IconStories}/>

# Icon

<br/>

## 사용 방법

`@seungchan-dev/icons` 패키지를 활용하여 아이콘 컴포넌트를 사용합니다

<Source language="tsx" code={`
import { Icon } from '@seungchan-dev/ui'
import { IconX } from '@seungchan-dev/icons'

<Icon source={IconX} />
`}/>

## Icon 목록

<IconGallery>
  {Object.keys(icons).map((name) => (
      <Icon source={icons[name]} />
  ))}
</IconGallery>
