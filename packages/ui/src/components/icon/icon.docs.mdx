import * as IconStories from './icon.stories'
import { Canvas, Controls, IconGallery, IconItem, Meta, Source, Story } from '@storybook/blocks'
import { Icon } from './icon'
import { icons } from '@seungchan-dev/icons'

<Meta of={IconStories} />

# Icon

<br />

## 사용 방법

`@seungchan-dev/icons` 패키지를 활용하여 아이콘 컴포넌트를 사용합니다

<Source language="tsx" code={`
import { Icon } from '@seungchan-dev/ui'
import { IconLogo } from '@seungchan-dev/icons'

<Icon source={IconLogo} />
`}/>

## Icon 목록

<IconGallery>
  {Object.keys(icons).map(name => (
    <IconItem key={name}>
      <Icon source={icons[name]} />
    </IconItem>
  ))}
</IconGallery>

## Icon Props

<Canvas>
  <Story />
</Canvas>

<Controls of={IconStories.Default} />
